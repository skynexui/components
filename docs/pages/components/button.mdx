---
title: Button
---
import { Canvas, Story, ArgsTable } from '../../scripts/docs/defaults.tsx'

import { Button } from '@skynexui/components';


# Button

Button is the component that you use, for get user taps and clicks.

```js
// JavaScript
import { Button } from '@skynexui/components';
```
```js
// Flutter
import 'package:skynexui_components/components.dart';
```
> [Wanna test the props? To go Storybook](https://storybook.skynexui.dev/?path=/docs/components-button--button-component)

<ArgsTable of={Button} />

<Canvas>
  <Story
    name="Button Component"
    args={{ ...Button.defaultProps, label: 'Button Component' }}
  >
    {(args) => <Button {...args} />}
  </Story>
</Canvas>

## With custom buttonColors

<Canvas>
  <Story name="Button Component with buttonColors">
    {(args) => (
      <Button
        variant="secondary"
        size="xl"
        label="Button Component with provided colors"
        buttonColors={{
          contrastColor: '#FFFFFF',
          mainColor: '#C65D21',
          mainColorLight: '#E67635',
          mainColorStrong: '#AB4E19',
        }}
        {...args}
      />
    )}
  </Story>
</Canvas>

## With icon and label

<Canvas>
  <Story
    name="Button Component with icon and label"
    args={{ ...Button.defaultProps, label: 'Button Component', iconName: 'github' }}
  >
    {(args) => <Button {...args} />}
  </Story>
</Canvas>


## With icon only

<Canvas>
  <Story
    name="Button Component with icon only"
    args={{ ...Button.defaultProps, iconName: 'arrowRight' }}
  >
    {(args) => <Button {...args} />}
  </Story>
</Canvas>

## With link external

<Canvas>
  <Story
    name="Button Component with link external"
    args={{ ...Button.defaultProps, label: 'Link to https://google.com', href: 'https://google.com' }}
  >
    {(args) => <Button {...args} />}
  </Story>
</Canvas>

## With link internal

<Canvas>
  <Story
    name="Button Component with link internal"
    args={{ ...Button.defaultProps, label: 'Link to /', href: '/' }}
  >
    {(args) => <Button {...args} />}
  </Story>
</Canvas>

      