import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { Provider, Text, TextField } from '@lib/components';

<Meta title="Components/Provider" />

# Provider

Provider is a wrapper that allows you to have enabled the facilities that the library has to offer in different environments

```js
// JavaScript
import { Provider } from '@skynexui/components';
```
```js
// Flutter
import 'package:skynexui_components/components.dart';
```
<!-- > [Wanna test the props? To go Storybook](https://storybook.skynexui.dev/?path=/docs/components-provider--provider-component) -->

<Canvas>
  <Story
    name="Provider Component"
    args={{
      ...Provider.defaultProps,
      theme: {
        components: {
          textField: {
            variant: 'bottomBorder',
          },
        },
      },
    }}
  >
    {(args) => (
      <Provider {...args}>
        <TextField
          label="Sample textfield with custom theme"
          value="try pass basicBordered to textField.variant"
        />
      </Provider>
    )}
  </Story>
</Canvas>
