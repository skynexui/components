import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { Box } from '@lib/components';


<Meta title="Components/Box" />

# Box

Box is the most primal component that we have that has the representation of a "box" in the UI.

<Canvas>
  <Story
    name="Box Component"
    args={{ children: 'Box Component' }}
  >
    <Box
      styleSheet={{
        backgroundColor: 'red',
        color: 'blue',
      }}
    >
      Box Component
    </Box> 
  </Story>
</Canvas>

